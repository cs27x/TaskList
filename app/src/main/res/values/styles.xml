<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Detail_TextViewStyle" parent="@android:style/Widget.TextView">
        <item name="android:layout_gravity">start|bottom</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">30dp</item>
    </style>

    <style name="Detail_InfoViewStyle" parent="@android:style/Widget.TextView">
        <item name="android:layout_gravity">start|bottom</item>
        <item name="android:textSize">20dp</item>
    </style>

    <style name="Detail_EditTextStyle" parent="@android:style/Widget.EditText">
        <item name="android:layout_gravity">start|bottom</item>
        <item name="android:hint">Hello World</item>
    </style>


</resources>
